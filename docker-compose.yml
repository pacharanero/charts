services:
  zensical:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8009:8000
    volumes:
      - .:/app
    command: zensical serve --dev-addr=0.0.0.0:8000
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
